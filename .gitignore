# Binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Go workspace
go.work
go.work.sum

# Output files
graph.bin
graph.bin.bak

# OSM data
malaysia-singapore-brunei-latest.osm.pbf

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Secrets
.env
.env.*
*.pem
*.key

# Time-metric staging graph + speed-table hash sidecar
graph.time.bin
*.speeds.sha256
/preprocess

# Elevete route-cache dataset — sensitive customer endpoints, generated outputs
# (CSVs, comparison results), Python bytecode, dated snapshots, and internal
# prod-extraction docs. Kept entirely out of the repo; the benchmark methodology
# is documented in docs/ and the harness lives locally.
datasets/
